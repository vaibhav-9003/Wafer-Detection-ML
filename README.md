# Wafer-Detection-ML
Machine Learning pipeline for wafer pass/fail prediction using Logistic Regression, XGBoost, SMOTE/ADASYN, and SHAP explainability — optimized for recall (F2-score) in imbalanced datasets
